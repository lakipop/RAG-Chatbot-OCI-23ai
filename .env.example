# This is an example file. Copy this to a new file named .env
# and fill in your actual credentials.

# IMPORTANT: The .env file contains secrets and should NEVER be committed to Git.
# That's why .env is listed in .gitignore

# ========================================
# OCI Configuration (Oracle Cloud Infrastructure)
# ========================================
# These values allow your Python app to authenticate with OCI services.
# You can find these in your OCI console under Identity > Users

# Your OCI user OCID (Oracle Cloud ID)
OCI_USER_ID="ocid1.user.oc1..aaaaaaaexampleuserid"

# Your OCI tenancy OCID (the root compartment of your cloud account)
OCI_TENANCY_ID="ocid1.tenancy.oc1..aaaaaaaexampletenancyid"

# The OCI region where your resources are located (e.g., us-chicago-1, us-ashburn-1)
OCI_REGION="us-chicago-1"

# The fingerprint of your API key (found in OCI console when you upload a public key)
OCI_KEY_FINGERPRINT="xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx"

# The path to your private key file (the .pem file you downloaded)
# On Windows, use forward slashes or double backslashes
# Example: C:/Users/YourName/.oci/oci_api_key.pem
OCI_PRIVATE_KEY_PATH="/path/to/your/.oci/oci_api_key.pem"

# The compartment OCID where your Generative AI resources will be created
COMPARTMENT_ID="ocid1.compartment.oc1..aaaaaaaexamplecompartmentid"

# ========================================
# Oracle 23ai Database Configuration
# ========================================
# These credentials connect your app to the Oracle Database where
# we'll store the vector embeddings (the "memory" of our RAG system)

# Database username (e.g., ADMIN for Autonomous Database)
DB_USER="ADMIN"

# Database password (you set this when creating the database)
DB_PASSWORD="YourDatabasePassword123!"

# Database DSN (Data Source Name) - this is the connection string
# For Autonomous Database, get this from OCI Console:
# Go to Autonomous Database > DB Connection > Connection Strings
# Use the "High" or "Medium" connection string
# Example format: (description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)...
DB_DSN="your_database_connection_string_here"
